import catalogReducer from './reducer';
import * as catalogTypes from './types';
import * as catalogActions from './actions';

export { catalogTypes, catalogActions };

export default catalogReducer;
